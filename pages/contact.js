import ContactRegister from "@/components/contact/ContactRegister";

// Export the ContactRegister component as the default export for the /contact route
export default function Contact() {
  return (
    <div>
      <ContactRegister />
    </div>
  );
}
